vector<int> parseInts(string str) {
    stringstream ss;
    ss << str;
    int input;
    vector<int> out;
    char ch;
    while(ss >> input)
    {
        out.push_back(input);
        ss >> ch;    
    }
    return out;
	// Complete this function
}
